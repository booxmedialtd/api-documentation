FavouriteChannelList:
  type: object
  properties:
    name:
      type: string
      example: "Kids channels"
      description: "A name that the user gives to the channel list when (s)he creates it"
    